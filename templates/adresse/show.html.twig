{% extends 'base.html.twig' %}

{% block title %}Adresse{% endblock %}
 {% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('./css/bootstrap.min.css') }}">
{% endblock %}
{% block body %}
    <h2  class="text-info">Adresse</h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adresse.id }}</td>
            </tr>
            <tr>
                <th>Rue</th>
                <td>{{ adresse.rue }}</td>
            </tr>
            <tr>
                <th>Code postal</th>
                <td>{{ adresse.codePostal }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ adresse.ville }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ adresse.pays }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('app_adresse_index') }}" class="btn btn-primary">Back to list</a>

    <a href="{{ path('app_adresse_edit', {'id': adresse.id}) }}" class="btn btn-primary">Edit</a>
<br><br>
    {{ include('adresse/_delete_form.html.twig') }}
{% endblock %}
