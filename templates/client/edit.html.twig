{% extends 'base.html.twig' %}

{% block title %}Edit Client{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <style>
        
        
         .back-link {
            text-decoration: none;
            color: #333;
            margin-top: 10px;
        }
        .form-txt{
            background: #f8f8f8;
                 padding: 50px;
            border: 1px solid #ccc;
            border-radius: 5px;
            width: 80%; 
            max-width: 1000px; 
            box-sizing: border-box; 
        }
        .btn-down{
            text-align:center;
        }
        .text-info{
            text-align:center;

        }
    </style>

{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
            <div class="form-txt">    
                            <h2 class="text-info">Edit Client</h2>

                {{ include('client/_form.html.twig', {'button_label': 'Update'}) }}
                <div class="btn-down">
                {{ include('client/_delete_form.html.twig') }}
                <a href="{{ path('app_client_index') }}" class="back-link"><i class="fas fa-arrow-left"></i>back to list</a>
                </div>
            </div>  
            </div>
        </div>
    </div>
{% endblock %}
