{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('./css/bootstrap.min.css') }}">
{% endblock %}
{% block body %}
    <h2 class="text-info" >Produit</h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ produit.nom }}</td>
            </tr>
              <tr>
                <th>Catégorie</th>
                <td>{{ produit.categorie }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ produit.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ produit.prix }}</td>
            </tr>
            <tr>
                <th>Qte</th>
                <td>{{ produit.qte }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td> <img  src={{produit.img }}  width="50" height="50"></td>
            </tr>
        </tbody>
    </table>

   <div class="btn-group btn-group-horizontal" role="group">
    <a class="btn btn-outline-info"href="{{ path('app_produit_index') }}">back to list</a>
    <a class="btn btn-warning" href="{{ path('app_produit_edit', {'id': produit.id}) }}">edit</a>
    {{ include('produit/_delete_form.html.twig') }}
</div>

{% endblock %}
